Delete an example from an intent
---
parameters:
  - name: project
    in: path
    type: string
    required: true
    default: development
    description: Name of the project to load an agent from
  - name: model
    in: path
    type: string
    required: true
    default: testing
    description: Name of the model within a project
  - name: id_intent
    in: path
    type: string
    required: true
    description: Id of the intent
  - name: id_template
    in: path
    type: string
    required: true
    description: Identifier of the text within the list of examples of the intent
responses:
  200:
    description: Return details of the intent with the text deleted